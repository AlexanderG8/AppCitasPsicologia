@using AppCitasPsicologia.ViewComponents
@model UsuarioInfoViewModel

<div class="w-8 h-8 rounded-full bg-indigo-500 flex items-center justify-center text-white text-sm font-semibold flex-shrink-0">@Model.Inicial</div>
<div class="min-w-0">
    <p class="text-sm font-medium text-white truncate">@Model.NombreCompleto</p>
    <p class="text-xs text-slate-500 truncate">@Model.NombreRol</p>
</div>
