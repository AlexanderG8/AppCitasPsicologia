@using AppCitasPsicologia.ViewComponents
@model UsuarioInfoViewModel

<div class="w-7 h-7 rounded-full bg-indigo-500 flex items-center justify-center text-white text-xs font-semibold">@Model.Inicial</div>
<span class="hidden sm:block text-sm font-medium">@Model.NombreCompleto</span>
